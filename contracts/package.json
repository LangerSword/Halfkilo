{
  "name": "halfkilo-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.ts --network fuji"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3",
    "@nomicfoundation/hardhat-viem": "^2",
    "hardhat": "^2.22.0",
    "typescript": "^5",
    "@types/node": "^20"
  }
}
